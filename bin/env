#!/bin/bash
#设置运行程序所需要的环境变量
#config this path to the actual application home path
PYTHON_VERSION=2.6
export APP_HOME=`dirname $0`/..
export CONFIG_PATH=${APP_HOME}/config
export PYTHONPATH=${APP_HOME}/src:$PYTHONPATH
#export PYTHONPATH=${APP_HOME}/lib${PYTHON_VERSION}:$PYTHONPATH
